apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: armory/kubesvc
    newTag: 0.2.0-rc.40    # Version of the agent to use

resources:
- deployment.yaml

configMapGenerator:
- name: kubesvc-config
  files:
  - kubesvc.yaml
