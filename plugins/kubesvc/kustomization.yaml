apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: armory/kubesvc
    newTag: 0.3.0    # Version of the agent to use

resources:
- deployment.yaml

configMapGenerator:
- name: kubesvc-config
  files:
  - kubesvc.yaml
