apiVersion: apps/v1
kind: Deployment
metadata:
  name: spin-dinghy
  labels:
    app.kubernetes.io/version: &version 2.26.0
spec:
  selector:
    matchLabels:
      app: spin
      cluster: spin-dinghy
  template:
    metadata:
      labels:
        app.kubernetes.io/version: *version
